$('#dialog_box').show();
$('#dialog_box').html("<%= escape_javascript(render partial: 'student_ell_preferences/form') %>");
$('#dialog_box').css({"top": $(window).scrollTop() + ($(window).height() / 10)});
