<% if @school.present? %>
  There was an error connecting to the <%= @endpoint %> endpoint with school id <%= @school.bps_id %> (<%= @school.name %>).

  The last sync for this school was <%= @school.last_synced %>

<% else %>
  There was an error connecting to the <%= @endpoint %> endpoint.
<% end %>