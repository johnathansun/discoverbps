$('#dialog_box').show();
$('#dialog_box').html("<%= escape_javascript(render partial: 'students/iep_needs', locals: {student: @student}) %>");
$('#dialog_box').css({"top": $(window).scrollTop() + ($(window).height() / 10)});
