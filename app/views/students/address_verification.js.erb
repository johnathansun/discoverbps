$('#dialog_box').show();
$('#dialog_box').html("<%= escape_javascript(render partial: 'schools/dialog_boxes/address_verification', locals: {student: @student, addresses: @addresses}) %>");
$('#dialog_box').css({"top": $(window).scrollTop() + ($(window).height() / 4)});
